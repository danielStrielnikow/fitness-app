server:
  port: 8082

spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ${MONGO_DATABASE}

eureka:
  client:
    eureka-url:
      defaultZone: ${EUREKA_URL}

base:
  url:
    user-service: http://USER-SERVICE